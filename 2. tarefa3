#include <iostream>

using namespace std;
float coefa(int num, float mult, float somax, float somay, float xx){
    float a;
    a=((num*mult)-(somax*somay))/((num*xx)-(somax*somax));
    return a;
}

float coefb(float somay, float xx, float somax, float mult, int num){
    float b;
    b=
    
}

int main()
{
    cout<<"quantidade de pontos";
    cin>>pont;
    
    while(k>pont){
        cout<<"coordenada x";
        cin>>x;
        
        cout<<"coordenada y";
        cin>>y;
        
        sx=sx+x;
        sy=sy+y;
        syx=syx+(x*y);
        sxx=sxx+(x*x);
        
    }

    return 0;
}
