#include<stdio.h>
#include<time.h>
#include<limits.h>
#define TAM 15

int calcula_maior(int vet[TAM]);

int main(){

    int vet[TAM], x, resultado;

    srand(time(NULL));

        for(x=0; x<TAM; x++){
            vet[x]=rand()%40;
        }

    resultado=calcula_maior(vet[TAM]);

    printf(" o maior numero= %d", resultado);

    return 0;

}


int  calcula_maior(int vet[TAM]){
      int resultado=INT_MIN;

      return resultado;


}
