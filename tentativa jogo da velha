#include<stdlib.h>
#define COL 3
#define LINHA 3
#include<stdio.h>
    
    int main(){

        int X, O,  linha, coluna, _, espaco;
        char marcador1;
        char mat[LINHA][COL];
        
        // escolher com qual simbolo cada jogador vai jogador
        
        
        printf("\n jogador 1 escolha entre X e O  ");
        scanf("%c", &marcador1);

        if(marcador1=='X'){
            printf("\n jogador 2 joga com O");
        }
        else if (marcador1!='X'){
            printf(" jogador 2 joga com X ");
        }
        
        // inserir a matriz vazia
         printf("\f");
         
        for(linha=1; linha<=LINHA; linha++){
            for(coluna=1; coluna<=COL; coluna++){
                mat[linha][coluna]= '-';
                
                printf("%c", mat[linha][coluna]);
                
            }
            printf("\n");
        }
        
        //começar o jogo
        printf("\n vamos comecar o jogo\f");
        
             for(espaco=1; espaco<=9; espaco++)

            printf("\n insira o numero da linha e coluna desejada ");
            scanf("%d%d", &linha &coluna);
	    mat[linha][coluna]='X';
            
            
            if(mat[linha][coluna]=='-'){
                if(espaco)
		
			if(linha>=0 && linha<=LINHA && coluna>=0 && coluna<=COL){
            mat[linha][coluna]='X';
            
            printf("\t%c", mat[linha][coluna]);
            }
            else{
                printf("movimento errado");
            }
            }

            printf("\n\n");

    return 0;
    }
